DateTime.format.front=d-MM-yyyy HH:mm
Date.format.db=EEEE, d MMM yyyy HH:mm
Time.format.db=HH:mm
Date.format=yyyy-MM-d


spring.check="work"	

#h2 datbase configuration
spring.datasource.url=jdbc:mysql://<databaseURL>/UserSchema
spring.datasource.username=<database username>
spring.datasource.password=<database password>
spring.jpa.hibernate.ddl-auto=update
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

#admin cred
Admin.UserName=admin
Admin.password=password

#jwt authentication token
jwt.secret="FlightApplication"

#edurkea client configuration
eureka.client.serviceurl.defaultZone= ${EUREKA_URI:http://localhost:8761/eureka}
eureka.instance.preferIpAddress= true
server.port=0


spring.application.name=USER-SERVICE

spring.jackson.serialization.fail-on-empty-beans=false 

#//java configuration for smptp service
#email configuration
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=<mail id for send mail>
#spring.mail.password=<gmail password for mail>

#//aws configuration for mailing service
spring.mail.host=<aws smtp host>
spring.mail.port=587
spring.mail.username=<aws smtp user>
spring.mail.password=<aws smtp password
from.email.address=<mail id to send mail>

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true